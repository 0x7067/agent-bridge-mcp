Read-only provider/profile comparison task:

Inspect this repository without editing files, running network operations, or changing local state.

Answer these questions from the checked-in files only:

1. What is the Agent Bridge MCP server's primary tool lifecycle?
2. Where are task transcripts and final result evidence exposed?
3. What local verification does the caller still own after provider output is available?

Keep the response concise and cite the files or tool names you used.
